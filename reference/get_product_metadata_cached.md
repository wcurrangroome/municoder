# Get product metadata with caching

Cached version of get_product_metadata(). Results are cached for 6
hours.

## Usage

``` r
get_product_metadata_cached(client_id, product_name)
```

## Arguments

- client_id:

  A unique identifier for a client

- product_name:

  The name of a product type
