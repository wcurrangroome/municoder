# Send a GET request to a municode API endpoint

Send a GET request to a municode API endpoint

## Usage

``` r
get_endpoint(endpoint, max_retries = 3)
```

## Arguments

- endpoint:

  An API endpoint URL

- max_retries:

  Maximum number of retry attempts (default 3)

## Value

Parsed JSON response
