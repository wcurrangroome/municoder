# Clear all cached API results

Clears all cached results from the \*\_cached() functions. Useful when
you need fresh data or want to free up memory.

## Usage

``` r
clear_cache()
```

## Examples

``` r
if (FALSE) { # \dontrun{
# Clear all cached data
clear_cache()
} # }
```
