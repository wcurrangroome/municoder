# State-related API functions Return metadata about states (and similar) as used by municode

Corresponds to: https://api.municode.com/States/

## Usage

``` r
get_states()
```

## Value

A dataframe with three columns: `state_id`, `state_name`,
`state_abbreviation.`
