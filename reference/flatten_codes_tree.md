# Recursively flatten a codesToc tree node into a tidy dataframe

Recursively flatten a codesToc tree node into a tidy dataframe

## Usage

``` r
flatten_codes_tree(node)
```

## Arguments

- node:

  A node list from the `codesToc/fullTree` response

## Value

A dataframe with one row per node in the subtree rooted at `node`
