# Get clients in a state with caching

Cached version of get_clients_in_state(). Results are cached for 24
hours.

## Usage

``` r
get_clients_in_state_cached(state_abbreviation)
```

## Arguments

- state_abbreviation:

  A two-character state code

## Value

A dataframe of clients
