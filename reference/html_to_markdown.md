# Convert HTML content to markdown, preserving table structure

Convert HTML content to markdown, preserving table structure

## Usage

``` r
html_to_markdown(html)
```

## Arguments

- html:

  A character vector of HTML strings

## Value

A character vector of markdown strings
