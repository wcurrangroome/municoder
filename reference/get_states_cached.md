# Get states with caching

Cached version of get_states(). States rarely change, so results are
cached indefinitely.

## Usage

``` r
get_states_cached()
```

## Value

A dataframe with three columns: `state_id`, `state_name`,
`state_abbreviation.`
