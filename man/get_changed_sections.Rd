% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{get_changed_sections}
\alias{get_changed_sections}
\title{List the sections that changed in a given version of an ordinance}
\usage{
get_changed_sections(job_id, product_id)
}
\arguments{
\item{job_id}{A unique identifier for a job}

\item{product_id}{A unique identifier for a product}
}
\value{
A dataframe with one row per changed section and the columns
\code{node_id}, \code{title}, \code{compare_status} (an undocumented change-type code),
and \code{ancestor_path} (the node's breadcrumb, titles joined by \code{" > "}).
Returns a zero-row dataframe when the version introduced no changes.
}
\description{
Corresponds to, for example: https://api.municode.com/CodesChangedDocs?jobId=494092&productId=15441
Returns the set of sections (nodes) that were added or amended in the
specified job relative to the preceding version. Useful for tracking
ordinance changes over time; pair with \code{\link[=get_version_history]{get_version_history()}} to walk
versions and \code{\link[=get_section_html]{get_section_html()}} (with \code{show_changes = TRUE}) to retrieve
the redline for each changed node.
}
\examples{
\dontrun{
get_changed_sections(job_id = 494092, product_id = 15441)
}
}
