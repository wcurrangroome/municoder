% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{get_codes_tree}
\alias{get_codes_tree}
\title{Get the full table of contents tree for an ordinance in a single request}
\usage{
get_codes_tree(job_id, node_id, product_id)
}
\arguments{
\item{job_id}{A unique identifier for a job}

\item{node_id}{A node to use as the root of the returned tree. Pass the
\code{product_id} to retrieve the whole code.}

\item{product_id}{A unique identifier for a product}
}
\value{
A dataframe with one row per node and the columns \code{id}, \code{heading},
\code{node_depth}, \code{parent_id}, \code{doc_order_id}, \code{has_children}, \code{node_key},
\code{is_updated}, \code{is_amended}, \code{has_amended_descendant}, \code{compare_status},
and \code{doc_type}.
}
\description{
Corresponds to, for example: https://api.municode.com/codesToc/fullTree?jobId=494092&nodeId=15441&productId=15441
Unlike \code{\link[=get_section_children]{get_section_children()}}, which returns one level of the hierarchy
per call, this returns the entire nested tree beneath \code{node_id} in a single
request and flattens it to a tidy dataframe (one row per node). Per-node
amendment flags are included.
}
\examples{
\dontrun{
get_codes_tree(job_id = 494092, node_id = 15441, product_id = 15441)
}
}
